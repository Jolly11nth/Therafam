import { useEffect, useState } from 'react';
import Brand from '../components/Brand';
import { supabase } from '../lib/supabase';
import { getStoredUserId } from '../lib/therafamApi';

type Props = { onBack: () => void; onHome: () => void };
type Panel = 'clients' | 'schedule' | 'profile' | 'notes' | 'availability' | 'messages' | null;
type ProfileForm = { professionalTitle: string; specialization: string; licenseNumber: string; yearsExperience: string; practiceLocation: string; bio: string };
type QueryResult<T> = { data: T | null; error: Error | null; count?: number | null };
const emptyProfile: ProfileForm = { professionalTitle: '', specialization: '', licenseNumber: '', yearsExperience: '', practiceLocation: '', bio: '' };

export default function TherapistPortal({ onBack, onHome }: Props) {
  const [clientCount, setClientCount] = useState(0), [sessionCount, setSessionCount] = useState(0), [panel, setPanel] = useState<Panel>(null);
  const [isActive, setIsActive] = useState(true), [savingStatus, setSavingStatus] = useState(false), [statusMessage, setStatusMessage] = useState('');
  const [profile, setProfile] = useState<ProfileForm>(emptyProfile), [profileDraft, setProfileDraft] = useState<ProfileForm>(emptyProfile);
  const [editingProfile, setEditingProfile] = useState(false), [savingProfile, setSavingProfile] = useState(false);
  const userId = getStoredUserId();

  useEffect(() => {
    if (!supabase || !userId) return;
    void (async () => {
      const [clients, sessions, therapist] = await Promise.all([
        supabase.from('therapist_client_relationships').select('id', { count: 'exact', head: true }).eq('therapist_id', userId).eq('status', 'active').execute(false) as Promise<QueryResult<null>>,
        supabase.from('therapy_sessions').select('id', { count: 'exact', head: true }).eq('therapist_id', userId).eq('status', 'scheduled').execute(false) as Promise<QueryResult<null>>,
        supabase.from('therapist_profiles').select('*').eq('user_id', userId).maybeSingle() as Promise<QueryResult<Record<string, unknown>>>,
      ]);
      setClientCount(clients.count ?? 0); setSessionCount(sessions.count ?? 0);
      const row = therapist.data;
      if (row) {
        const next: ProfileForm = { professionalTitle: String(row.professional_title ?? ''), specialization: String(row.specialization ?? ''), licenseNumber: String(row.license_number ?? ''), yearsExperience: row.years_experience == null ? '' : String(row.years_experience), practiceLocation: String(row.practice_location ?? ''), bio: String(row.bio ?? '') };
        setProfile(next); setProfileDraft(next); if (row.is_accepting_clients != null) setIsActive(Boolean(row.is_accepting_clients));
      }
    })();
  }, [userId]);

  useEffect(() => { if (supabase && userId) return; const saved = localStorage.getItem('therafam:therapist-active'); if (saved !== null) setIsActive(saved === 'true'); const savedProfile = localStorage.getItem('therafam:therapist-profile'); if (savedProfile) try { const next = JSON.parse(savedProfile) as ProfileForm; setProfile(next); setProfileDraft(next); } catch { /* ignore */ } }, [userId]);

  async function toggleActive() { const next = !isActive; setIsActive(next); setSavingStatus(true); setStatusMessage(''); try { if (supabase && userId) { const { error } = await supabase.from('therapist_profiles').update({ is_accepting_clients: next, updated_at: new Date().toISOString() }).execute(false); if (error) throw error; } else localStorage.setItem('therafam:therapist-active', String(next)); setStatusMessage(next ? 'You are now accepting clients.' : 'You are now unavailable for new clients.'); } catch (error) { setIsActive(!next); setStatusMessage(error instanceof Error ? error.message : 'Could not update availability.'); } finally { setSavingStatus(false); } }
  function openPanel(next: Panel) { setPanel(next); setStatusMessage(''); if (next === 'profile') { setProfileDraft(profile); setEditingProfile(false); } }
  async function saveProfile(event: React.FormEvent) { event.preventDefault(); setSavingProfile(true); setStatusMessage(''); try { const next = { ...profileDraft, yearsExperience: profileDraft.yearsExperience.trim() }; if (supabase && userId) { const { error } = await supabase.from('therapist_profiles').upsert({ user_id: userId, professional_title: next.professionalTitle.trim(), specialization: next.specialization.trim(), license_number: next.licenseNumber.trim(), years_experience: next.yearsExperience === '' ? null : Number(next.yearsExperience), practice_location: next.practiceLocation.trim(), bio: next.bio.trim(), updated_at: new Date().toISOString() }, { onConflict: 'user_id' }).execute(false); if (error) throw error; } else localStorage.setItem('therafam:therapist-profile', JSON.stringify(next)); setProfile(next); setProfileDraft(next); setEditingProfile(false); setStatusMessage('Professional profile updated successfully.'); } catch (error) { setStatusMessage(error instanceof Error ? error.message : 'Could not save your profile.'); } finally { setSavingProfile(false); } }

  return <section className="workspace-shell therapist-workspace"><header className="workspace-topbar"><button className="workspace-brand" onClick={onHome} aria-label="Go home"><Brand compact /></button><div className="therapist-topbar-actions"><button className="text-link" onClick={() => openPanel('profile')}>My profile</button><button className="text-link" onClick={onBack}>Return to client area</button></div></header><div className="workspace-content"><div className="workspace-heading"><div><span className="eyebrow">Professional portal</span><h1>Therapist workspace</h1><p>Manage your client relationships, appointments, professional profile, and availability.</p></div><div className="therapist-status-control"><span className={`status-dot ${isActive ? 'online' : 'offline'}`} aria-hidden="true"/><div><strong>{isActive ? 'Active' : 'Offline'}</strong><small>{isActive ? 'Accepting new clients' : 'Not accepting new clients'}</small></div><button className={`status-toggle ${isActive ? 'on' : 'off'}`} onClick={toggleActive} disabled={savingStatus} aria-pressed={isActive}><span/></button></div></div>{statusMessage && <div className="status-feedback" role="status">{statusMessage}</div>}<div className="dashboard-grid"><article className="dashboard-card"><span className="eyebrow">Active clients</span><strong className="workspace-stat">{clientCount}</strong><p>Clients currently assigned to your care.</p><button className="outline-action" onClick={() => openPanel('clients')}>View clients</button></article><article className="dashboard-card"><span className="eyebrow">Upcoming sessions</span><strong className="workspace-stat">{sessionCount}</strong><p>Scheduled sessions requiring attention.</p><button className="outline-action" onClick={() => openPanel('schedule')}>View schedule</button></article><article className="dashboard-card"><span className="eyebrow">Professional status</span><strong className="status-text">{isActive ? 'Active' : 'Offline'}</strong><p>{isActive ? 'You are available for new client requests.' : 'You are unavailable for new client requests.'}</p><button className="outline-action" onClick={() => openPanel('profile')}>Edit professional profile</button></article></div><div className="therapist-columns"><section className="dashboard-card"><div className="card-heading"><strong>Today</strong><span>Schedule</span></div><div className="empty-state compact"><span>◷</span><strong>{sessionCount ? `${sessionCount} scheduled session${sessionCount === 1 ? '' : 's'}` : 'No sessions loaded'}</strong><p>Open your schedule to review appointment details.</p><button className="outline-action" onClick={() => openPanel('schedule')}>Open schedule</button></div></section><section className="dashboard-card"><div className="card-heading"><strong>Care workflow</strong><span>Secure</span></div><div className="workflow-list"><button onClick={() => openPanel('notes')}>Review client notes <span>→</span></button><button onClick={() => openPanel('availability')}>Update availability <span>→</span></button><button onClick={() => openPanel('messages')}>Open secure messages <span>→</span></button></div></section></div>{panel && <section className="settings-card therapist-action-panel"><div className="support-panel-header"><div><span className="eyebrow">Therapist workspace</span><h2>{panelTitle(panel)}</h2></div><button className="icon-action" onClick={() => setPanel(null)} aria-label="Close">×</button></div>{panel === 'profile' ? <form className="therapist-profile-editor" onSubmit={saveProfile}><div className="profile-editor-grid"><Field label="Professional title" value={profileDraft.professionalTitle} disabled={!editingProfile} onChange={v => setProfileDraft({ ...profileDraft, professionalTitle: v })}/><Field label="Specialization" value={profileDraft.specialization} disabled={!editingProfile} onChange={v => setProfileDraft({ ...profileDraft, specialization: v })}/><Field label="Registration / license number" value={profileDraft.licenseNumber} disabled={!editingProfile} onChange={v => setProfileDraft({ ...profileDraft, licenseNumber: v })}/><Field label="Years of experience" type="number" value={profileDraft.yearsExperience} disabled={!editingProfile} onChange={v => setProfileDraft({ ...profileDraft, yearsExperience: v })}/><Field label="Country / State of practice" value={profileDraft.practiceLocation} disabled={!editingProfile} onChange={v => setProfileDraft({ ...profileDraft, practiceLocation: v })}/></div><label className="therapist-field">Professional bio<textarea rows={5} maxLength={500} disabled={!editingProfile} value={profileDraft.bio} onChange={e => setProfileDraft({ ...profileDraft, bio: e.target.value })}/></label><div className="panel-actions">{!editingProfile ? <button type="button" className="primary-action" onClick={() => setEditingProfile(true)}>Edit profile</button> : <><button type="submit" className="primary-action" disabled={savingProfile}>{savingProfile ? 'Saving…' : 'Save changes'}</button><button type="button" className="outline-action" onClick={() => { setProfileDraft(profile); setEditingProfile(false); }}>Cancel</button></>}<button type="button" className="outline-action" onClick={() => setPanel(null)}>Close</button></div></form> : <><p>{panelCopy(panel)}</p><div className="panel-actions"><button className="primary-action" onClick={() => panel === 'availability' ? toggleActive() : setStatusMessage('This workspace is connected and ready for its backend data.')}>{panel === 'availability' ? (isActive ? 'Set offline' : 'Set active') : 'Refresh'}</button><button className="outline-action" onClick={() => setPanel(null)}>Close</button></div></>}</section>}</div></section>;
}
function Field({label,value,disabled,onChange,type='text'}:{label:string;value:string;disabled:boolean;onChange:(value:string)=>void;type?:string}) { return <label className="therapist-field">{label}<input type={type} value={value} disabled={disabled} onChange={e => onChange(e.target.value)} /></label>; }
function panelTitle(panel: Exclude<Panel, null>) { return panel === 'clients' ? 'Client relationships' : panel === 'schedule' ? 'Session schedule' : panel === 'profile' ? 'Professional profile' : panel === 'notes' ? 'Client notes' : panel === 'availability' ? 'Availability' : 'Secure messages'; }
function panelCopy(panel: Exclude<Panel, null>) { if (panel === 'clients') return 'Your active client list will appear here when therapist relationships are connected to your account.'; if (panel === 'schedule') return 'Your scheduled therapy sessions will appear here when appointment data is connected.'; if (panel === 'notes') return 'Client notes are kept behind the authenticated therapist workflow.'; if (panel === 'availability') return 'Control whether new clients can request your services.'; return 'Secure therapist-client messages will appear here when the messaging workflow is connected.'; }
