-- =============================================================================
-- THERAFAM - LOCAL DEVELOPMENT SEED DATA
-- =============================================================================
-- This file is used by `supabase db reset` for local development
-- It runs after all migrations are applied
-- =============================================================================

-- This seed file is intentionally minimal as the main seed data
-- is in the migrations/20250111000002_seed_data.sql file

-- You can add additional local-only test data here if needed
-- For example:

-- INSERT INTO users (email, password_hash, user_type, is_verified) VALUES
-- ('test@example.com', '$2a$10$examplehash', 'client', true);

-- Any data added here will be reset each time you run `supabase db reset`
